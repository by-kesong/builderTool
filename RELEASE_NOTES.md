- **远端分支拉取到本地**：重新获取分支时自动执行远端同步；远端分支列表支持双击或右键“拉取此分支到本地”，并在本地已存在同名分支时显示标识，避免重复判断。
- **分支配置教育产品线**：新增“数据分析软件”“可视化软件”教育产品线选项，配置弹窗按“产业 | 实训 | 教育产品线”分区展示，教学部署表格同步显示可点击的“教育”标签。
- **教学部署记录去重**：不同项目创建同一个新分支名时，教学部署版面按分支名只保留一条记录，重复创建会更新已有记录而不是新增重复行。
- **IP + Logo 一键替换**：打包配置区拆分为 IP 与 Logo 两行；教育旧项目支持上传学校 Logo、预览 Logo，并在“一键替换”时同步替换 IP 与 `src/images/loginLogo.xxx`，自动更新 `src/views/Home.vue` 中的后缀引用。
- **部署图片多图管理**：教学部署图片支持多选上传、多图预览、预览层删除当前图片；表格删除按钮保留为清空该记录全部图片，避免误删单张图片。
- **主题与界面细节修复**：修复浅色主题启动时顶部导航栏样式不同步的问题；优化 Logo 区域在非教育旧项目下的隐藏逻辑，减少无效占位。